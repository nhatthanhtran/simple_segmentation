name: segmentation_env
channels:
  - nvidia
  - pytorch
  - conda-forge
dependencies:
  - python=3.10
  - pytorch=2.2
  - torchvision
  - pytorch-cuda=11.8
  - numpy=1.26.4
  - pillow
  - opencv
  - pip
  - pip:
      - ultralytics
      - pillow-heif
      - tqdm
      - matplotlib
      - einops
      - transformers<4.40